/**
 * Application-level configuration for Nuxt UI.
 * Defines the primary color theme and toast notification behavior.
 */
export default defineAppConfig({
  ui: {
    colors: {
      primary: 'blue',
      neutral: 'zinc'
    }
  }
})
